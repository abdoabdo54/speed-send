# Global environment configuration for docker-compose (copy to .env and edit)

# Database
POSTGRES_USER=gmailsaas
POSTGRES_PASSWORD=gmailsaas123
POSTGRES_DB=gmail_saas

# App secrets (change in production!)
SECRET_KEY=please-change-this-secret
ENCRYPTION_KEY=32-bytes-long-encryption-key-please

# SMTP (optional)
SMTP_ENABLED=false
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_USE_TLS=true

# Frontend to backend URL
NEXT_PUBLIC_API_URL=http://localhost:8000

# Message-ID Domain
MESSAGE_ID_DOMAIN=example.local
